
set(EXE_NAME extractMSLesion)


SEMMacroBuildCLI(
NAME ${EXE_NAME}
EXECUTABLE_ONLY
ADDITIONAL_SRCS 
TARGET_LIBRARIES ${ITK_LIBRARIES}
LINK_DIRECTORIES ""
INCLUDE_DIRECTORIES ""
INSTALL_RUNTIME_DESTINATION ${INSTALL_RUNTIME_DESTINATION}
INSTALL_LIBRARY_DESTINATION ${INSTALL_LIBRARY_DESTINATION}
INSTALL_ARCHIVE_DESTINATION ${INSTALL_ARCHIVE_DESTINATION}
)

set(EXE_NAME castVectorImage)


SEMMacroBuildCLI(
NAME ${EXE_NAME}
EXECUTABLE_ONLY
ADDITIONAL_SRCS 
TARGET_LIBRARIES ${ITK_LIBRARIES}
LINK_DIRECTORIES ""
INCLUDE_DIRECTORIES ""
INSTALL_RUNTIME_DESTINATION ${INSTALL_RUNTIME_DESTINATION}
INSTALL_LIBRARY_DESTINATION ${INSTALL_LIBRARY_DESTINATION}
INSTALL_ARCHIVE_DESTINATION ${INSTALL_ARCHIVE_DESTINATION}
)


set(EXE_NAME msLesionStats)


SEMMacroBuildCLI(
NAME ${EXE_NAME}
EXECUTABLE_ONLY
ADDITIONAL_SRCS 
TARGET_LIBRARIES ${ITK_LIBRARIES}
LINK_DIRECTORIES ""
INCLUDE_DIRECTORIES ""
INSTALL_RUNTIME_DESTINATION ${INSTALL_RUNTIME_DESTINATION}
INSTALL_LIBRARY_DESTINATION ${INSTALL_LIBRARY_DESTINATION}
INSTALL_ARCHIVE_DESTINATION ${INSTALL_ARCHIVE_DESTINATION}
)

set(EXE_NAME msLesionEvaluate)


SEMMacroBuildCLI(
NAME ${EXE_NAME}
EXECUTABLE_ONLY
ADDITIONAL_SRCS 
TARGET_LIBRARIES ${ITK_LIBRARIES} ${PYTHON_LIBRARIES}
LINK_DIRECTORIES ""
INCLUDE_DIRECTORIES ""
INSTALL_RUNTIME_DESTINATION ${INSTALL_RUNTIME_DESTINATION}
INSTALL_LIBRARY_DESTINATION ${INSTALL_LIBRARY_DESTINATION}
INSTALL_ARCHIVE_DESTINATION ${INSTALL_ARCHIVE_DESTINATION}
)